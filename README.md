# Fake News Detection System

An AI-powered application that classifies news articles as **Real** or **Fake** using Natural Language Processing (NLP) and Machine Learning.

## 🚀 Features
- **Text Cleaning:** Removes noise, punctuation, and stop-words.
- **Vectorization:** Uses TF-IDF to convert text to numerical data.
- **Model:** Passive Aggressive Classifier for high-speed accuracy.
- **UI:** Interactive web interface built with Streamlit.

## 🛠️ Installation
1. Clone the repo: `git clone https://github.com/your-username/repo-name.git`
2. Install requirements: `pip install -r requirements.txt`
3. Run Preprocessing: `python src/preprocess.py`
4. Run Training: `python src/model_train.py`
5. Start App: `streamlit run app.py`

## 📊 Dataset
The project uses the **ISOT Fake News Dataset** containing over 40,000 articles.
